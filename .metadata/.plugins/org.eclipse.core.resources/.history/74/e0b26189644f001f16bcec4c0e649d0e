
html, body {
    height: 100%;
    margin: 0;
}

/* Background styling for the html */
html {
    background-image: url('../images/hero-earth.jpg');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
}

/* Centering the content and setting up styling for body */
body {
    display: flex;
    justify-content: center;
    align-items: center;
}

/* Styling for the container */
#wrap {
    width: 480px;
    padding: 20px;
    background-color: rgba(0, 14, 51, 0.7);
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
    transform: translateY(-100px);
}

/* General styling for form elements */
form > div {
    text-align: center;
    padding: 10px;
    font-size: 20px;
    font-weight: 600;
    color: white;
}

/* Styling for the header */
h1 {
    text-align: center;
    color: white;
}

/* Styling for input fields and select elements */
input[type="email"],
input[type="password"],
input[type="text"],
input[type="file"],
select {
    width: calc(100% - 40px);
    padding: 10px;
    margin: 10px 0;
    font-size: 16px;
}

/* Styling for submit button */
input[type="submit"] {
    padding: 10px 20px;
    font-size: 18px;
    font-weight: 600;
    background-color: #007BFF;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

/* Hover effect for submit button */
input[type="submit"]:hover {
    background-color: #0056b3;
}
